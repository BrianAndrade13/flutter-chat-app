import 'package:flutter/material.dart';

class UsuariosPages extends StatelessWidget {
  const UsuariosPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Usuarios Page'),
      centerTitle: true,
      ),
      body: Center(
        child: Text('Hola Mundo'),
      ),
    );
  }
}